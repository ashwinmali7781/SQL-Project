# ⭐ Student Registration Form.


# 🧑‍🎓 Flask Student Registration Application

This is a basic **CRUD (Create and Read)** application built with **Flask** and **MySQL** for managing student registration records.

The application allows users to register new students via a web form and displays a list of all currently registered students from the database.

---

## 🚀 Setup and Installation

### 1. Prerequisites

* **Python**
* **Flask** web framework.
* **`mysql-connector-python`** library.
* **MySQL Server** running on your local machine or a remote host.

### 2. Install Dependencies

Install the required Python packages using pip:

```bash
pip install Flask mysql-connector-python
